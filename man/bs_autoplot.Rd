% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bs_autoplot.R
\name{bs_autoplot}
\alias{bs_autoplot}
\title{Autoplot species range shifts from the BioShifts database}
\usage{
bs_autoplot(
  data = NULL,
  plottype = c("point", "boxplot", "map"),
  facet = NULL,
  polygon_folder = "./BioShiftR_polygons"
)
}
\arguments{
\item{data}{Either a character vector of species names (e.g.
\code{"Troglodytes_troglodytes"}, spaces or underscores accepted) \emph{or} a
range-shift data frame from \code{get_shifts()}.  When omitted the full
database is used.}

\item{plottype}{One of \code{"point"} (per-study dot plot with group means),
\code{"boxplot"} (distribution of rates by range parameter), or \code{"map"}
(species polygons on a world map, coloured by shift rate).  Partial
matching is supported.}

\item{facet}{Optional column name (string) in \code{data} to use as a
\code{facet_wrap()} grouping variable.}

\item{polygon_folder}{Path to locally-downloaded polygon files from
\code{download_polygons(type = "SP")}.  Only used when \code{plottype = "map"}.
Defaults to \code{"./BioShiftR_polygons"}.}
}
\value{
A ggplot2 object.
}
\description{
Quickly visualise range shift data in one of three ways: a per-study dot
plot, a distribution by range parameter, or a spatial polygon map.
}
\details{
Currently not an exported function.

The function accepts input in two ways:
\itemize{
\item \strong{By species name} – pass a character vector of one or more
\code{sp_name_checked} values as the first argument.  The full database is
queried automatically.
\item \strong{Pipe-in} – filter \code{get_shifts()} yourself (e.g., to species) and pipe
the result in.
}
}
\examples{
\dontrun{
# Quick single-species dot plot
bs_autoplot("Troglodytes_troglodytes", plottype = "point")

# Two species boxplot at once
bs_autoplot(c("Troglodytes_troglodytes", "Fringilla_coelebs"),
  plottype = "boxplot", facet = "sp_name_checked"
)

# Pipe-in style
get_shifts(group = "Birds", continent = "Europe") |>
  bs_autoplot(plottype = "point")


# Map requires downloaded polygons (see ?download_polygons)
download_polygons(type = "SP")
bs_autoplot("Troglodytes_troglodytes", plottype = "map")
}
}
\keyword{internal}
